import numpy as np
import matplotlib.pyplot as plt

import os
import sys

from astropy.wcs import WCS

def main():

    # Create a fake fits file with the correct WCS 
    # We need a fits file with a correctly defined WCS
    # to start the astropy.WCS related plotting.
    # Could probably create the WCS object directly...

    

	return None

if __name__ == '__main__':
	main()
	sys.exit(0)